/target

# Local Claude Code settings — machine-specific, not part of the project
.claude/settings.local.json

# Python bytecode from the benchmark harness
__pycache__/
*.pyc

# Local MCP overrides. Real endpoints belong here, never in the tracked
# .mcp.json — a production host committed to a public repo cannot be recalled.
.mcp.local.json
